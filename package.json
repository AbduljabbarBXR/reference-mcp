{
  "name": "reference-mcp",
  "version": "0.1.0",
  "description": "Reference integrity for AI agents. Scans a repository for URLs, imports, assets, and dependencies, then verifies each reference resolves.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "reference-mcp": "src/index.js"
  },
  "files": [
    "src"
  ],
  "keywords": ["mcp", "model-context-protocol", "agents", "tawakkul-labs"],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
